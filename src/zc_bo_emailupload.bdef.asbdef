projection implementation in class ZBP_C_BO_EMAILUPLOAD unique;
strict ( 2 );
use draft;
define behavior for ZC_BO_EMAILUPLOAD alias ZcBoEmailupload
use etag

{
  use create;
  use update;
  use delete;

  use action fileUpload;
  use function downloadFile;
  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}